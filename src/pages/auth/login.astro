---
// Permanent redirect: this page was renamed to /auth/signin so the page
// name matches the corresponding API handler at /api/auth/signin.
// Preserving query string so ?next=<path> and ?error=<msg> survive.
const target = '/auth/signin' + Astro.url.search;
return Astro.redirect(target, 301);
---
